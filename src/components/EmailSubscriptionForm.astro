<script async src="https://subscribe-forms.beehiiv.com/embed.js">
</script>
<iframe src="https://subscribe-forms.beehiiv.com/276e232b-dce4-4f20-bd22-8c76d6c1d1cc" 
  class="beehiiv-embed" 
  data-test-id="beehiiv-embed" 
  frameborder="0" 
  scrolling="no" 
  style="width: 100%; max-width: 515px; height: 162px; margin: 0; border-radius: 0px 0px 0px 0px !important; background-color: transparent; box-shadow: 0 0 #0000;"></iframe>

    <!-- <form 
  class="flex flex-col sm:flex-row gap-3" 
  id="subscribeForm"
>
  <input 
    type="email" 
    name="email"
    placeholder="Masukkan alamat email" 
    required
    class="flex-1 px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary text-text"
  />
  <button 
    type="submit"
    class="bg-primary text-white px-6 py-3 rounded-xl font-medium hover:bg-blue-600 transition-colors focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2 flex items-center justify-center min-w-[120px]"
    id="submitButton"
  >
    <span>Langganan</span>
    <svg class="hidden animate-spin ml-2 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" id="loadingIcon">
      <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
      <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
    </svg>
  </button>
</form>

<div class="hidden text-green-600 font-medium mt-3" id="successMessage">
  ✓ Terima kasih! Email Anda telah terdaftar.
</div>

<p class="text-gray-500 mt-3 text-sm" id="defaultMessage">
  Bergabung dengan 1.000+ solopreneur yang mendapatkan insight AI mingguan. Tanpa spam, berhenti kapan saja.
</p>

<script is:inline>
const FORM_URL = 'https://script.google.com/macros/s/AKfycbwQpFwZbBJtTaEu4yT2UwxJfukE017PISQRj4wQRB4oj61ZHR6MvLDbtvZWVGIL4eI/exec';

document.getElementById('subscribeForm').addEventListener('submit', async (e) => {
  e.preventDefault();
  
  const form   = e.target;
  const email  = form.email.value.trim();
  if (!email) return;

  // UI refs
  const btn          = document.getElementById('submitButton');
  const icon         = document.getElementById('loadingIcon');
  const btnLabel     = btn.querySelector('span');
  const successMsg   = document.getElementById('successMessage');
  const defaultMsg   = document.getElementById('defaultMessage');

  // loading state
  btn.disabled = true;
  icon.classList.remove('hidden');
  btnLabel.textContent = 'Mengirim...';

  try {
    const body = new FormData();
    body.append('email', email);

    await fetch(FORM_URL, { method: 'POST', body });

    // success state
    form.reset();
    successMsg.classList.remove('hidden');
    defaultMsg.classList.add('hidden');
    btn.classList.replace('bg-primary', 'bg-green-600');
    icon.classList.add('hidden');
    btnLabel.textContent = '✓ Berhasil';

    // reset UI after 3 s
    setTimeout(() => {
      btn.disabled = false;
      btn.classList.replace('bg-green-600', 'bg-primary');
      btnLabel.textContent = 'Langganan';
      successMsg.classList.add('hidden');
      defaultMsg.classList.remove('hidden');
    }, 3000);
  } catch (err) {
    console.error(err);
    btn.disabled = false;
    icon.classList.add('hidden');
    btnLabel.textContent = 'Langganan';
    alert('Terjadi kesalahan. Silakan coba lagi.');
  }
});
</script> -->
